import { StyleSheet } from 'react-native';

const styles = {
    container: {
        flex: 1,
        flexDirection: 'column'
    },
    dateText: {
        padding: 5,
        fontWeight: 'bold'
    }
};

export default StyleSheet.create(styles);